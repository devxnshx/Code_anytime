// Read two integers from user input and display their sum.
#include <stdio.h>

int main() {
    int a, b;
    printf("enter two numbers: ");
    scanf("%d%d",&a,&b);

    printf("the sum of numbers u entered is : %d\n",a+b);
     return 0;
}