// Input two floating-point numbers and print their product
#include <stdio.h>

int main() {
    float a,b;
    printf("enter two floating point numbers");
    scanf("%f%f",&a,&b);
    float c=a*b;
    printf("%.2f",c);
     return 0;
}
